import React from 'react';
import LoadingIcon from '../../aseets/images/loading.svg';

import { Spinner } from './styles';

const Loading = () => <Spinner src={LoadingIcon} alt="Loading" />;

export default Loading;
